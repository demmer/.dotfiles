#!/bin/csh

setenv TERMINFO ~/.terminfo
if ($TERM == "" || $TERM == "xterm") then
    setenv TERM xterm-color
endif
stty stop  "^\'"           # unset ctrl-s
stty lnext "^\'"           # unset ctrl-v
exec /usr/local/bin/mutt $argv
