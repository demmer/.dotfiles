#!/bin/sh

PROG='mailbox'

if [ "$HOST" != "haight.nbttech.com" ]; then
	PROG='ssh -q haight -o BatchMode=yes mailbox'
fi

OPTS="-geometry 270x45+812+73 unread count delay=1 mailbox=/var/mail/demmer"
RED="< /dev/null > /dev/null"

$PROG $OPTS $RED &
